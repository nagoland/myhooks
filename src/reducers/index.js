

const events = (state=[], action) => {

    switch(action.type){
        case "SEND_MESSAGE":
            return state
        case "DELETE_MESSAGE":
            return state
        case "DELETE_ALL_MESSAGE"
        default:
            return state
    }

}

export default events